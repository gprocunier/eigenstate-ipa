---
- name: Render certificate inventory report
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Render certificate inventory report artifacts
      ansible.builtin.import_role:
        name: eigenstate.ipa.certificate_inventory_report
