---
- name: Render policy drift report
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Render policy drift report artifacts
      ansible.builtin.import_role:
        name: eigenstate.ipa.policy_drift_report
