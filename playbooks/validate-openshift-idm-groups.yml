---
- name: Validate OpenShift IdM group inputs
  hosts: localhost
  gather_facts: false
  roles:
    - role: eigenstate.ipa.openshift_idm_oidc_validation
      vars:
        eigenstate_oidc_render_config: false
        eigenstate_oidc_render_report: true
