# temporary_access_report

Renders read-only evidence for temporary access windows. The role is intended
for scheduled review and handoff reporting after access has been opened,
expired, revoked, or scheduled by a separate workflow.

The report object uses the schema identifier
`eigenstate.ipa/temporary_access_report/v1`.
