# policy_drift_report

Renders a read-only comparison between expected and observed policy records.
The role does not remediate drift. Each finding identifies the separate
opt-in remediation workflow that would make a change.

The report object uses the schema identifier
`eigenstate.ipa/policy_drift_report/v1`.
